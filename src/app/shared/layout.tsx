import "@/app/globals.css";

export const metadata = {
  title: "Shared Report | RANZ Roofing",
  description: "View shared roofing inspection report",
};

export default function SharedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
